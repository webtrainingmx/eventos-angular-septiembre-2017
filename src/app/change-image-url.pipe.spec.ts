import { ChangeImageURLPipe } from './change-image-url.pipe';

describe('ChangeImageURLPipe', () => {
  it('create an instance', () => {
    const pipe = new ChangeImageURLPipe();
    expect(pipe).toBeTruthy();
  });
});
